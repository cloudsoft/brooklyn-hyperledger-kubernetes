location:
  kubernetes:
    endpoint: http://ec2-52-59-250-72.eu-central-1.compute.amazonaws.com:8000
    identity: admin
    credential: admin
services:
  - type: hyperledger-fabric-single-cluster
    brooklyn.config:
      hyperledger.app.timeout: 120
      hyperledger.peers: 2
