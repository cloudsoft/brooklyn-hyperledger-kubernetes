apiVersion: v1
kind: Service
metadata:
  name: hyperledger-membersrvc-node-service
  namespace: hyperledger-fabric
  labels:
    app: hyperledger-fabric
    role: membersrvc
spec:
  ports:
    - name: "ecap"
      port: 7054
      targetPort: 7054
  selector:
    app: hyperledger-fabric
    role: membersrvc
